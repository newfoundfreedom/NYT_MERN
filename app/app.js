// Require Dependencies
const React = require('react'),
    ReactDOM = require('react-dom'),
    Main = require('./components/Main.js');

// Render Main component to index page
ReactDOM.render(<Main />, document.getElementById('app'));

